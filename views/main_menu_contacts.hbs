<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Контакти</title>
    <link rel="stylesheet" href="/css/table.css">
    <link rel="stylesheet" href="/css/buttons.css">
    <link rel="stylesheet" href="/css/main_styles.css">
    <link rel="stylesheet" href="/css/input_form.css">
    <script src="https://kit.fontawesome.com/b99e675b6e.js"></script>
  </head>
  <body>
    
    <header class="header">
      <h1>Редагування контактів</h1>
      <a id="exit" href="/admin/{{user}}/exit"><i class="fas fa-power-off"></i> ВИХІД</a>
    </header>
    {{> sidebar id=user}}
    <div class="table_container">
      {{!-- <div class="control-panel small">
        <a href="/admin/{{user}}/contact/add" class="button accent-btn">Додати</a>
      </div> --}}
      <div class="content-area">
        <form action="/admin/{{user}}/contact/send" method="POST">
          <div>
            <label for="email" class="label">Пошта</label>
            <input type="email" class="stretch" name="email" value="{{email}}" id="email" required>
          </div>
          <div>
            <label for="phone" class="label">Телефон</label>
            <input type="phone" class="stretch" name="phone" value="{{phone}}" id="phone" required>
          </div>
          <div>
            <label for="address" class="label">Адреса</label>
            <textarea name="address" id="address" required>{{address}}</textarea>
          </div>
          <button type="submit" class="button primary-btn"> Зберегти</button>
        {{!-- <table class="table-fill">
          <thead>
            <tr>
              <th>Назва</th>
              <th>Контакт</th>
              <th>Тип Контакту</th>
              <th>Дії</th>
            </tr>
          </thead>
          <tbody class="table-hover">
            {{#each contacts}}
              <tr>
                <td class="text-left">{{title}}</td>
                <td class="text-left">{{contact}}</td>
                <td class="text-left">{{type}}</td>
                <td class="left-text">
                  <form method="GET" action="/admin/{{../user}}/contact/edit/{{_id}}">
                    <button type="submit" class="button primary-btn">Редагувати</button>
                  </form>
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
        <ul>
          {{#loop 1 pages 1}}
            <li id="pages-container">
              <a id="counter" href="./{{this}}">{{this}}</a>
            </li>
          {{/loop}}
        </ul> --}}
      </div>
    </div>   
  </body>
</html>
