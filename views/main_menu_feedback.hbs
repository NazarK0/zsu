<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Вхідні Повідомлення</title>
    <link rel="stylesheet" href="/css/table.css">
    <link rel="stylesheet" href="/css/buttons.css">
    <link rel="stylesheet" href="/css/main_styles.css">
    <script src="https://kit.fontawesome.com/b99e675b6e.js"></script>
  </head>
  <body>
    <header class="header">
      <h1>Вхідні Повідомлення</h1>
      <a id="exit" href="/admin/{{user}}/exit"><i class="fas fa-power-off"></i> ВИХІД</a>
    </header> 
    {{> sidebar id=user}}
    <div class="table_container">
      <div class="control-panel small">
        {{!-- <a href="/admin/{{user}}/email/add" class="button accent-btn">Додати аккаунт</a> --}}
      </div>
      <div class="content-area">
        <table class="table-fill">
          <thead>
            <tr>
              <th>Адреса</th>
              <th>Дата</th>
              <th>Дії</th>
            </tr>
          </thead>
          <tbody class="table-hover">
            {{#each message}}
              <tr>
                <td class="text-left">{{email}}</td>
                <td class="text-center">{{date}}</td>
                <td class="text-center">
                  <form method="GET" action="/admin/{{../user}}/message/view/{{_id}}">
                  <button id="remake"><i class="fas fa-pencil-alt"></i>Переглянути</button>
                  </form>
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
        <ul>
          {{#loop 1 pages 1}}
            <li id="pages-container">
              <a id="counter" href="./{{this}}">{{this}}</a>
            </li>
          {{/loop}}
        </ul>
      </div>
    </div>     
  </body>
</html>
