<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Архів Повідомлень</title>
    <link rel="stylesheet" href="/css/table.css">
    <link rel="stylesheet" href="/css/buttons.css">
    <link rel="stylesheet" href="/css/main_styles.css">
    <script src="https://kit.fontawesome.com/b99e675b6e.js"></script>
  </head>
  <body>
    <header class="header">
      <h1>Архів Повідомлень</h1>
      <a id="exit" href="/admin/{{user}}/exit"><i class="fas fa-power-off"></i> ВИХІД</a>
    </header>
    {{> sidebar id=user}}
    <div class="table_container">
      <div class="control-panel small">
        <a href="/admin/{{user}}/archive_message/xlsx" class="button secondary-btn inline">Зберегти .xls</a>
        <form method="POST" action="/admin/{{user}}/archive/clear" class="inline">
          <button type="submit" class="button delete-btn">Очистити Архів</button>
        </form>
      </div>
      <div class="content-area">
        <table class="table-fill">
          <thead>
            <tr>
              <th>Адреса відправника запиту</th>
              <th>Дата відправлення</th>
              <th>Логін відповідача</th>
              <th>Дата відповіді</th>
              <th>Переглянути</th>
            </tr>
          </thead>
          <tbody class="table-hover">
            {{#each archive}}
              <tr>
                <td class="text-left">{{email}}</td>
                <td class="text-center">{{date}}</td>
                <td class="text-left">{{login_response}}</td>
                <td class="text-center">{{date_response}}</td>
                <td class="text-center">
                  <form method="GET" action="/admin/{{../user}}/archive/view/{{_id}}">
                  <button type="submit" class="button secondary-btn">Переглянути</button>
                  </form>
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
        <ul>
          {{#loop 1 pages 1}}
            <li id="pages-container">
              <a id="counter" href="./{{this}}">{{this}}</a>
            </li>
          {{/loop}}
        </ul>
      </div>
    </div>     
  </body>
</html>
