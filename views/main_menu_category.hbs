<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Категорії</title>
    <link rel="stylesheet" href="/css/table.css">
    <link rel="stylesheet" href="/css/resets.css">
    <link rel="stylesheet" href="/css/buttons.css">
    <link rel="stylesheet" href="/css/main_styles.css">
    <script src="https://kit.fontawesome.com/b99e675b6e.js"></script>
  </head>
  <body>
    <header class="header">
      <h1>Категорії для Новин</h1>
      <a id="exit" href="/admin/{{user}}/exit"><i class="fas fa-power-off"></i> ВИХІД</a>
    </header> 
    {{> sidebar id=user}}
    
    <div class="table_container">
      <div class="control-panel small">
        <a href="/admin/{{user}}/news/category/add" class="button accent-btn">Додати Категорію</a>
      </div>
    
      <div class="content-area">
          <form action="/admin/{{user}}/news/category/photo-editor">
            <button class="button accent-btn" >Перейти до  Фото</button>
          </form>
          <form action="/admin/{{user}}/news/1">
            <button class="button accent-btn" >Перейти до  Новин</button>
          </form>
        
        <table class="table-fill">
          <thead>
            <tr>
              <th>Емблема</th>
              <th>Назва Українською</th>
              <th>Назва Англійською</th>
              <th>Дії</th>
            </tr>
          </thead>
          <tbody class="table-hover">
            {{#each category}}
              <tr>
                <td class="text-center"><img src="data:image/jpeg;base64,{{photo}}" alt="{{title}}" height="60px"></td>
                <td class="text-left">{{title_ua}}</td>
                <td class="text-left">{{title_en}}</td>
                <td class="text-center">
                  <form method="GET" action="/admin/{{../user}}/news/category/edit/{{_id}}">
                    <button type="submit" class="button primary-btn">Редагувати</button>
                  </form>
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
        <ul>
          {{#loop 1 pages 1}}
            <li id="pages-container">
              <a id="counter" href="./{{this}}">{{this}}</a>
            </li>
          {{/loop}}
        </ul>
      </div>
    </div>
  </body>
</html>
